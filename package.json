{
    "name": "labelstudio-dashboard",
    "version": "1.0.0",
    "private": true,
    "description": "Label Studio Dashboard - Unified Backend + Frontend",
    "scripts": {
        "dev": "concurrently -n \"backend,frontend\" -c \"bgBlue.bold,bgMagenta.bold\" \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
        "start": "npm run dev",
        "backend": "npm start --prefix backend",
        "frontend": "npm run dev --prefix frontend",
        "build": "npm run build --prefix frontend",
        "build:frontend": "npm run build --prefix frontend",
        "install:all": "npm install --prefix backend && npm install --prefix frontend"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}